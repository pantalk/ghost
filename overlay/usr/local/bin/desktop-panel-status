#!/bin/bash
# Render the compact Pantalk daemon state shown by the Tint2 panel executor.

set -euo pipefail

if pgrep -u "$(id -u)" -x pantalkd >/dev/null 2>&1; then
    printf '<span foreground="#24DBC9">●</span> running\n'
else
    printf '<span foreground="#818C98">●</span> stopped\n'
fi
