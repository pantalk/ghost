services:
  ghost:
    image: pantalk/ghost:ergo-dev
    pull_policy: build
    build:
      context: ../..
      dockerfile: Dockerfile
      target: ghost
      args:
        TARGETARCH: amd64
        PANTALK_VERSION: ${PANTALK_VERSION:-0.0.11}
